version: '3.8'
services:
  omnimail:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
      - DATABASE_PATH=/data/omnimail.db
    volumes:
      - omnimail-data:/data
    restart: always

volumes:
  omnimail-data:
